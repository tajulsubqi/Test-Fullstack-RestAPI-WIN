/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "via.placeholder.com",
      "upload.wikimedia.org",
      "cworld.id",
      "images.tokopedia.net",
      "www.static-src.com",
    ],
  },
}

export default nextConfig
